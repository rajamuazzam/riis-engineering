# RIIS Engineering V1.2.1

Routes:
- `/` — Industry Gateway
- `/offshore/` — exact V1.1.1 Native Refined website
- `/onshore/` — Civil & Infrastructure scroll website

The Offshore route hosts the original Native Refined static build unchanged inside a full-viewport native page container. Its original scroll, modal, images and styling are preserved.

Cloudflare Pages:
- Build command: `npm run build`
- Output directory: `out`
