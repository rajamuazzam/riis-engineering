# RIIS V1.1.1 Native Refined Build

This revision improves the native coded website against the approved visual concept.

Main corrections:
- Tight-cropped platform asset so the hero graphic is large, not tiny
- Hero and section heights recalibrated to the concept proportions
- Header, typography, buttons, service cards, credentials, experience and footer spacing refined
- Standalone Tools strip removed; Tools navigation anchors to the capability/credentials area
- Print CSS added so PDF review does not create a blank first page
- All text, buttons, cards, navigation and layout remain native HTML/CSS/JS

Cloudflare Pages:
- Build command: npm run build
- Output directory: out
