RIIS V2.1 — Native Offshore Page

Changes:
- Replaces the Offshore iframe with a native Next.js page.
- Uses the same shared RIIS header and footer as Home, Onshore, FEA and PMT.
- Preserves the Offshore technical content and visual direction.
- No package or Cloudflare configuration changes.

Upload the contents of this ZIP to the repository root and overwrite existing files.
The old app/offshore/page.js must be deleted. GitHub upload may not delete it automatically, so delete it manually if it remains after upload.
